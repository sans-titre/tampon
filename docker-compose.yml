services:
  atelier:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./tirages:/app/tirages
      - ./logs:/app/logs
      - ~/.local/share/fonts:/usr/local/share/fonts:ro
    cap_add:
      - SYS_ADMIN
    restart: unless-stopped
